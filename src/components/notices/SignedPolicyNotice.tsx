'use client'

import React from 'react'

export const SignedPolicyNotice = () => (
  <div className="mt-2 p-2 bg-yellow-50 border border-yellow-200 rounded text-sm text-yellow-800">
    <strong>Important:</strong> When using Signed Playback Policy, you must provide both Signing Key
    ID and Signing Key Private Key below. These are required for signed playback to work properly.
  </div>
)
